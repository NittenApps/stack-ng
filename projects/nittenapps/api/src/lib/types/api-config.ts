/**
 * Configuration values used to construct API and file URLs.
 */
export type ApiConfig = {
  /** Base URL for API requests. */
  baseUrl: string;
  /** Base URL for file resources. */
  filesBase: string;
};
